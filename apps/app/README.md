# Guidence

```yaml
install-watchman:
  {
    for: ["linux", "macOs"],
    whatToDo:
      {
        doc: "https://facebook.github.io/watchman/docs/install.html",
        steps:
          [
            "download :=> executable file and install",
            "download :=> source-code (zip)",
            "extract & follow setups from documentation",
          ],
      },
  }
```
